let numbers = [12, 34, 54, 65, 76, 87];
console.log(`${numbers.push}`);

const a = 12;
const b = 12;
const sum = `Sum of ${a} and ${b} is : ${a+b}`
console.log(sum);

const student = {
    name: 'sakib',
    id: 1,
    class: 10,
    roll: 12,
    address: 'Dhaka',
}
const data = `students name is ${student.name} and id is ${student.id}`;
console.log(data)